# Database (MySQL)
spring.datasource.url = jdbc:mysql://localhost:3307/springdocker_db
spring.datasource.username = user_springdocker
spring.datasource.password = Springdocker123456?xyz
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
# Disable throwing exceptions when a bean is empty with no content
spring.datasource.serialization.fail-on-empty-beans = false
# For debugging
spring.jpa.show-sql: true